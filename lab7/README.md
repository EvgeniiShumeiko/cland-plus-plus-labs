# lab7

ЛР7: наследование и виртуальные функции (SDL2-версия).

Файлы:
- `shapes.h` — иерархия `Point`/`Circle`/`Ring`
- `task1.cpp` — анимация фигур
- `task2.cpp` — изменение ширины кольца и цвета

Сборка (пример):
`g++ task1.cpp -std=c++17 -lSDL2 -o out/task1`
