# lab4

ЛР4: работа со строками.

Файлы:
- `task1.cpp` — функции `strlen/strcpy/strcat/strcmp`
- `task2.cpp` — собственные функции строк
- `task3.cpp` — динамическая память через `malloc`
- `task4.cpp` — сравнение `malloc` и `calloc`
- `task5.cpp` — массив строк

Сборка примера:
`g++ task2.cpp -std=c++17 -o out/task2`
